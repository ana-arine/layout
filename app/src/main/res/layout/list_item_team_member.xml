<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="@dimen/list_item_height">

  <ImageView
      android:id="@+id/teamMemberAvatar2"
      android:layout_width="@dimen/list_item_height"
      android:layout_height="@dimen/list_item_height"
      android:layout_alignParentStart="true"
      android:layout_alignParentTop="true"
      tools:srcCompat="@tools:sample/avatars"
      android:contentDescription="@string/contentDescription_teamMemberAvatar"/>

  <TextView
    android:id="@+id/teamMemberLogin2"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="@dimen/padding_standard"
    android:textSize="@dimen/login_text_size"
      android:layout_toEndOf="@id/teamMemberAvatar2"
      android:layout_centerVertical="true"
    tools:text="login" />

  <TextView
      android:id="@+id/teamMemberType2"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_toEndOf="@id/teamMemberLogin2"
      android:layout_alignParentEnd="true"
      android:layout_marginStart="@dimen/padding_standard"
      android:layout_marginEnd="@dimen/padding_standard"
      android:text="TextView"
      android:textSize="@dimen/type_text_size"
      android:gravity="end"
      android:layout_alignBaseline="@id/teamMemberLogin2"/>

</RelativeLayout>